# Personal Website Project

## Core Instructions

**Dev Server**: Run `npm run dev` locally. No preview tool screenshots. Use relative paths in `public/` HTML (e.g., `../projects`). All copy: use `/my-voice` skill. Base URL staging: `/personal-site`.

## Key Constraints

Astro + Tailwind site. Static HTML in `public/` uses relative links. Site URL: joflei.github.io/personal-site.
